{
  "name": "abyssal-archon",
  "version": "1.0.0",
  "description": "Demonic Idle Progression Game",
  "scripts": {
    "dev": "vite",
    "build": "vite build && rm -rf dist/assets/images && mkdir -p dist/assets && cp -R assets/images dist/assets/images",
    "preview": "vite preview"
  },
  "devDependencies": {
    "eslint": "^9.39.5",
    "vite": "^8.1.5"
  }
}
